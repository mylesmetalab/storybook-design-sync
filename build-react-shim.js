// Auto-injected by esbuild so classic-JSX `React.createElement(...)` calls
// resolve to the host Storybook's React module (kept external in the bundle).
import * as React from "react";
export { React };
